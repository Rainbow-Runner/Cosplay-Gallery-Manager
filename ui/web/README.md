# Cosplay Gallery Manager Web

这是独立的React 19前端入口，用于新的BrowseShell、ManageShell和SetupShell。新业务不得继续添加到`ui/v2.5`。

当前阶段只提供可构建、可测试的应用壳层。旧UI继续维持过渡期构建，直到新前端通过全部功能门禁后再从发行包移除。

常用命令：

```bash
pnpm install
pnpm run validate
pnpm run dev
```
